// for(let i=0; i<5;i++)
// {
//     console.log("Hello World!");
// }

// while(i>5)
// {
//     console.log("ujjawal saini");
// }

// do
// {
//    console.log("ujjawal saini");
// }while(i>5);

// for(let i of strVal)   it is used to itterate over stings and arrays it out update the i variable
// {

// }

// for(let var in objVar)  it is used to itterate over an objects and arrays it gives you the keys in that variable (var) it does not pass value in var we can acces the value using the keys  objVar[var] <-- syntax
// {
//     // do some work with var 
// }


//                                 ------strings------

let str = "ujjawal";

// string in javascript are immutabe we can not update the value at any paticular index what we can do is crete a new string and copy it in the previous one 

// str.length -- > return the length of sting 

// indices of a sting   str[0], str[1];

// template litterals    let var = `this is a template litterar ${var1} ${var2} it convers the variables and expression in string only`;

// escape charecters /n /t  the are a single charecter not 2 diffrent charecters 

// use MDN refrence to study all the string methods 
