//                                          ----- arrays ------

let arr = [1,2,3,4,5,6,7,8,9];

// console.log(arr);  --> prints the array 

// arr[3] --> we can access individual index like this an we can update it to 

// arr.length --> gives the length of the array 

// for(let i of arr)
// {
//     console.log(i);      -->  we can also use for in loop 
// }

//                     -----array methods-----

// push(data1,data2,data3)  -> push at end and returns the length of new array
//pop()  ->> dleate from the end and return the deleated item
// tostring() ->> convert the string into a string and return a string  '1,2,3,4' -->this form
// arr1.concat(arr2,arr3,arr4 ....) -> return a new array doesnt change the original one it adds two arrays 
// array3 = array2 + array1 -> converts to string and add them and arrray3 is a string at the end
